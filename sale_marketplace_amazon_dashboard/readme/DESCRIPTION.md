This module adds a KPI dashboard aggregating the Amazon marketplace suite:
orders, listings and buy-box win rate, recent price changes and offers, fee-aware
margin, FBA inventory drift, settlement reconciliation, returns, and an overall
health status with drill-down actions.

The dashboard can be scoped to a single channel or aggregate all Amazon
channels.
