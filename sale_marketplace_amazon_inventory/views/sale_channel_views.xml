<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2026 Ledo
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>
    <record id="sale_channel_view_form" model="ir.ui.view">
        <field name="model">sale.channel</field>
        <field name="inherit_id" ref="sale_marketplace_amazon.sale_channel_view_form" />
        <field name="arch" type="xml">
            <button name="action_amazon_test_connection" position="after">
                <button
                    name="action_amazon_push_inventory"
                    type="object"
                    string="Push Inventory"
                    class="btn-secondary"
                    invisible="channel_type != 'amazon' or not inventory_sync_enabled"
                />
            </button>
            <group name="amazon_config" position="after">
                <group
                    name="amazon_inventory"
                    string="Amazon Inventory (FBM)"
                    invisible="channel_type != 'amazon'"
                >
                    <field name="inventory_sync_enabled" />
                    <field
                        name="inventory_expose_ratio"
                        invisible="not inventory_sync_enabled"
                    />
                    <field
                        name="inventory_min_reserve"
                        invisible="not inventory_sync_enabled"
                    />
                    <field
                        name="inventory_location_ids"
                        widget="many2many_tags"
                        invisible="not inventory_sync_enabled"
                    />
                    <field name="last_inventory_sync_date" />
                </group>
            </group>
        </field>
    </record>
</odoo>
